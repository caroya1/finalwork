spring:
  application:
    name: dianping-gateway
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 192.168.145.128:8848
        username: ${spring.cloud.nacos.username}
        password: ${spring.cloud.nacos.password}
        context-path: /
        group: DEFAULT_GROUP
      config:
        server-addr: 192.168.145.128:8848
        username: ${spring.cloud.nacos.username}
        password: ${spring.cloud.nacos.password}
        context-path: /
        file-extension: yml
        group: DEFAULT_GROUP
