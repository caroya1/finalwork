server:
  port: 8081

spring:
  application:
    name: dianping-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.145.128:8848
      config:
        server-addr: 192.168.145.128:8848
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
